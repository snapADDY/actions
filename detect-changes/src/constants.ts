export enum Inputs {
  ChangeKey = 'change-key',
  PersistRun = 'persist-run',
}

export enum Outputs {
  ChangedFiles = 'changed-files',
  LastCommitSHA = 'last-commit-sha',
}

export const KEY_PREFIX = 'actions-detect-changes-';
